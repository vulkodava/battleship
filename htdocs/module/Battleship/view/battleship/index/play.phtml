<div class="row">
    <div class="col-lg-12">
        <h1>
            <?php echo sprintf(
                $this->translate('Game #%d played by %s #%d'),
                $game->getGameEntity()->getId(),
                $game->getGameEntity()->getPlayer()->getFirstName() . ' ' . $game->getGameEntity()->getPlayer()->getLastName(),
                $game->getGameEntity()->getPlayer()->getId()
            ); ?>
        </h1>
    </div>
</div>

<?php $gameConfig = $game->getGameConfig(); ?>
<div class="row">
    <div class="col-lg-6">
        <table border="1" class="game">
        <?php $colHeaders = $gameGrid[0]; ?>
            <thead>
                <tr>
                    <?php
                    echo '<th></th>';
                    foreach ($colHeaders as $colHeaderKey => $colHeaderVal) {
                        echo '<th>' . ($colHeaderKey + 1) . '</th>';
                    }
                    ?>
                </tr>
            </thead>
            <?php foreach ($gameGrid as $rowId => $row) : ?>
                <tr>
                    <td><?php echo \Battleship\Repository\Game::$letters[$rowId]; ?></td>
                    <?php foreach ($row as $colId => $col) : ?>
                    <td>
                        <?php echo $this->partial('battleship/partials/field_plate_' . $col['field_plate_status'], array(
                            'col' => $col,
                            'colId' => $colId,
                            'row' => $row,
                            'rowId' => $rowId,
                            'cheat' => $cheat,
                            'gameConfig' => $gameConfig,
                        )); ?>
                    </td>
                    <?php endforeach; ?>
                </tr>
            <?php endforeach; ?>
        </table>
    </div>

    <?php echo $this->partial('battleship/partials/game_info'); ?>
</div>